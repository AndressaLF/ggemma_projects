#!/bin/sh
# Remove linhas de co-autoria do Cursor antes de cada commit.
MSG_FILE=$1

if [ -z "$MSG_FILE" ] || [ ! -f "$MSG_FILE" ]; then
  exit 0
fi

grep -v 'cursoragent@cursor\.com' "$MSG_FILE" > "${MSG_FILE}.tmp" || true
mv "${MSG_FILE}.tmp" "$MSG_FILE"

exit 0
